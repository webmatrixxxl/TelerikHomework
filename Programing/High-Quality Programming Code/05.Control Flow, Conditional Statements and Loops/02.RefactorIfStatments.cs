Potato potato = new Potato();

if (potato != null)
{
    if (potato.isPeeled && !potato.isRotten)
    {
        Cook(potato);
    }
}

if (x == minX && y == minY && !isVisitedCell)
{
    VisitCell();
}